# Domain Docs

This repo uses one primary context file:

- `CONTEXT.md`

Architecture decisions live in:

- `docs/adr/`

Spec-driven work lives in:

- `docs/specs/`

Additional AI navigation docs live in:

- `docs/ai/`

Frontend context is external rather than inside this repo:

- Frontend repo: `D:\toeic-vocab-app`
- Frontend AI contract: `D:\toeic-vocab-app\docs\ai\api-contract.md`

Agents should read `CONTEXT.md` first, then follow the task-specific "Read next by task" section.

